{
  "name": "appointment-availability-monitor",
  "version": "1.0.0",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node src/server.js",
    "check": "node --check src/*.js scripts/*.js test/*.js watch-icbc.js",
    "test": "node --test",
    "check:once": "node scripts/check-once.js"
  },
  "keywords": ["playwright", "automation", "email", "appointment-monitor"],
  "author": "Alexandra Zhiznyakova",
  "license": "MIT",
  "description": "A multi-user ICBC appointment availability monitor with email notifications.",
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "playwright": "^1.59.1"
  }
}
